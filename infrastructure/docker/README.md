# infrastructure/docker

**Responsibility:** Container definitions and Docker-related assets for SitePilot AI.

## Scope

- Application Dockerfiles (when apps are implemented)
- Shared base image notes
- Compose fragments for local/staging overlays

## Status

Scaffold only. No Dockerfiles or production images yet.

Root `docker-compose.yml` orchestrates local services at the repo root.
