# infrastructure/terraform

**Responsibility:** Infrastructure as Code for SitePilot AI cloud environments.

## Scope

- Networking, compute, and datastore modules (planned)
- Environment workspaces (dev / staging / prod)
- Secrets wiring and IAM least privilege

## Status

Scaffold only. No Terraform modules or state backends yet.

Do not commit `.tfstate` files (see root `.gitignore`).
