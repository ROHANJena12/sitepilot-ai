# infrastructure/nginx

**Responsibility:** Reverse proxy, TLS termination, and routing configuration for SitePilot AI.

## Scope

- Upstream definitions for `web` and `api`
- TLS / certificate hooks
- Rate limiting and security headers (planned)

## Status

Scaffold only. No Nginx configs committed yet.
