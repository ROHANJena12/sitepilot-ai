# packages/config

**Responsibility:** Shared tooling and configuration presets for the monorepo.

## Scope

- ESLint / Prettier / TypeScript base configs
- Shared environment / feature-flag config shapes
- Package export conventions

## Consumers

- All `apps/*` and `packages/*`

## Status

Scaffold only. No config presets are implemented yet.
