/**
 * TODO: Implement pure calculation helpers (score aggregates, deltas, clamps).
 * TODO: Do not place business rules that belong in the API or feature layer here.
 */

// TODO: export function clamp(value: number, min: number, max: number): number
// TODO: export function percentChange(current: number, previous: number): number

export {};
